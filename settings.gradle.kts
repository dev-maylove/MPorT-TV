pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.name = "MPorT-TV"
include(":app")

// Enable configuration cache related features when available
enableFeaturePreview("TYPESAFE_PROJECT_ACCESSORS")
